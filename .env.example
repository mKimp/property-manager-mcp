# Turso (libSQL) credentials — get these after provisioning at turso.tech
# turso db show <db-name> --url   → copy the libsql:// URL
# turso db tokens create <db-name> → copy the token

TURSO_DATABASE_URL=libsql://your-db-name-your-org.turso.io
TURSO_AUTH_TOKEN=your-auth-token-here

# Prisma migrations use DATABASE_URL — embed the auth token in the URL
# Format: libsql://<host>?authToken=<token>
DATABASE_URL=libsql://your-db-name-your-org.turso.io?authToken=your-auth-token-here
